#include <iostream>

int main () {
    
    double a;
    std::cout << "Enter the number: ";

    if(a > 0 ){
        std::cout << a <<" is a positive numeber" << std::endl;
    } else {
        std::cout << a <<" is a negative numeber" << std::endl;
    }

    return 0;
}